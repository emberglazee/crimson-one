[package]
name = "crimson_markov"
version = "0.1.0"
edition = "2021"

[dependencies]
fastrand = "2.0.1"
regex = "1.5" # For advanced tokenization
string-interner = "0.19.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.145"

[lib]
crate-type = ["cdylib"]
